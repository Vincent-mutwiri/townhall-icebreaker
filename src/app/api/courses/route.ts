// src/app/api/courses/route.ts
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'Courses API - Under Construction' });
}

export async function POST() {
  return NextResponse.json({ message: 'Create Course API - Under Construction' });
}
